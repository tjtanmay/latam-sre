apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flightapp-chart.fullname" . }}-test-connection"
  labels:
    {{- include "flightapp-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flightapp-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

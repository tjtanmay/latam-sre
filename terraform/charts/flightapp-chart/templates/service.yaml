apiVersion: v1
kind: Service
metadata:
  name: {{ include "flightapp-chart.fullname" . }}
  labels:
    {{- include "flightapp-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flightapp-chart.selectorLabels" . | nindent 4 }}
